export enum signEnum {
    signIn,
    signUp,
    error_field,
    add_field,
    submit_form,
    login = 'Se connecter',
    logout = "s'enregistrer",
    errorField = 'Error champs, verifier la lnogueur des caratères',
}

export enum Colors {
    red = '#e50916',
    white = '#fff',
}
